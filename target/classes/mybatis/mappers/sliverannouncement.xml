<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="himedia.slivermate.mappers.SliverAnnouncementMapper">
  
  <!-- 기존 SELECT 메서드 -->
  <select id="selectAllAnnouncement" 
          resultType="himedia.slivermate.repository.vo.SliverAnnouncement">
    SELECT * FROM sliver_announcement ORDER BY announce_id DESC
  </select>
  
  <!-- 수정된 INSERT 메서드 예시 -->
  <insert id="insertAnnouncement" 
          parameterType="himedia.slivermate.repository.vo.SliverAnnouncement"
          useGeneratedKeys="true" keyProperty="announceId">
    INSERT INTO sliver_announcement
      (announce_title, announce_date, announce_time, announce_location,
       announce_desc, meeting_price, attend_count, announce_type, upd_date)
    VALUES
      (#{announceTitle}, #{announceDate}, #{announceTime}, #{announceLocation},
       #{announceDesc}, #{meetingPrice}, #{attendCount}, #{announceType}, #{updDate})
  </insert>
  
</mapper>
